# include <stdio.h>
# include <cs50.h>

int main(void)
{
    int array[100], swap,n,c,x,y

    n = get_int("enter the number of elements:")

    for(c=0; c<n; c++)

    x = get_int("enter a number:")

    for(c=0;c<n-1;c++)

    position = c;
    swap = 

    for(y=0; y<n; y++)
}